Youtube 이야기's G

prototype: 원형
생산 비용이 높은 인스턴스(종류가 너무 많아서 클래스로 정리되지 않는 경우, 클래스로부터 인스턴스 생성이 어려운 경우)를 복사를 통해 쉽게 생산.

Youtube GIS DEVELOPER

실행 중에 생성된 객체로 다른 객체를 생성하는 패턴.
prototype으로 만든 객체의 상태를 변경해도 원본 객체의 상태는 변경되지 않는다.(깊은 복사)

새로운 클래스를 추가하지 않고도 새로운 개념의 객체를 생성할 수 있다면 프로토타입 패턴을 적용해서 유연성을 발휘할 수 있다.

 blog
 Code: Cloneable_Car
 코드가 복사해야 하는 구현 클래스에 의존하지 않아야 하는 경우 ex_코드가 인터페이스를 통해 써드파티 코드와 함께 작동할 경우
 객체를 초기화 하는 방식만 달라 서브클래스의 수를 줄이려는 경우 프로토타입 패널을 사용

 장
 -구현 클래스에 직접 연결하지 않고 객체를 복사할 수 있다.
 -프로토타입이 미리 정의되어 있기 때문에 중복되는 초기화 코드를 제거할 수 있다.
 -복잡한 오브젝트를 편리하게 만들 수 있습니다.

 단
 -순환 참조(?)가 있는 복잡한 객체를 복제하는 것은 매우 까다로울 수 있다.

 *순환참조: